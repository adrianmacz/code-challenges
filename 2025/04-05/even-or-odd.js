function evenOrOdd(number) {
  if (number % 2 === 0) {
    return "Even";
  }
  return "Odd";
}

//modulus is the remainder operator returns the remainder left over when one operand is divided by a second operand.
// It always takes the sign of the dividend
